# PaperRead

这是一个用于管理论文阅读笔记的仓库。

## 目录结构

- `WildDet3D/`：WildDet3D 论文阅读笔记与 LaTeX 工程
- `AttnRes/`：AttnRes 相关论文阅读笔记

## 使用方式

各子目录可以独立维护自己的 LaTeX 或笔记文件。

例如，在 `WildDet3D/` 目录下可使用以下命令编译：

```powershell
latexmk -xelatex main.tex
```
